<UserControl x:Class="EarlyPusher.Modules.EarlySettingTab.Views.EarlySettingTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EarlyPusher.Modules.EarlySettingTab.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:xcd="http://schemas.xceed.com/wpf/xaml/toolkit"
             d:DesignHeight="600"
             d:DesignWidth="800"
             mc:Ignorable="d">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="2*" />
		</Grid.ColumnDefinitions>
		<Menu Grid.Row="0"
		      Grid.Column="0"
		      Grid.ColumnSpan="3">
			<MenuItem Header="セット追加" />
			<MenuItem Header="セット削除" />
		</Menu>

		<ListBox Grid.Row="1"
		         Grid.Column="0"
		         ItemsSource="{Binding Sets}"
		         SelectedItem="{Binding SelectedSet}" />
		<GridSplitter Grid.Row="1"
		              Grid.Column="1"
		              Width="3"
		              ResizeBehavior="PreviousAndNext" />
		<StackPanel Grid.Row="1"
		            Grid.Column="2"
		            Margin="10">

			<Label Content="セット名" />
			<TextBox Text="{Binding SetName}" />

			<Label Content="フォルダ" />
			<TextBox IsReadOnly="True" />
			<Button Content="参照" />

			<Label Content="基準ポイント" />
			<xcd:IntegerUpDown Height="20" Value="{Binding BasicPoint}" />
		</StackPanel>
	</Grid>
</UserControl>
